class Solution {
    public int findPeakElement(int[] nums) {
            int max=Integer.MIN_VALUE,index=0;
            for(int j=0;j<nums.length;j++){
                if(nums[j]>max){
                    max =nums[j];
                    index=j;
                }}
                return index;
            }
        }
